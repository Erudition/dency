# Payload CMS
PAYLOAD_SECRET=6c63db79af25713a563191e8
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# PostgreSQL (must match docker-compose.yml)
POSTGRES_URL=postgres://payload:payload@localhost:5432/residency

# Set to "true" to seed the database on startup
SEED_DB=true
